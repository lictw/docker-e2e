version: '3'
services:
    redis:
        image: redis
    web-application:
        build: web-application
        environment:
            - PORT=8080
            - REDIS_HOST=redis
        depends_on:
            - redis
